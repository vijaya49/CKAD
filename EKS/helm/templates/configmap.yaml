apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "multi-apps.fullname" . }}-config
  labels:
    {{- include "multi-apps.labels" . | nindent 4 }}
data:
  # Add any common configuration here
  timezone: "UTC"
  log-level: "info"